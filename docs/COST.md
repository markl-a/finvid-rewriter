# 成本假設與決策說明

所有單價集中在 [`pipeline/pricing.py`](../pipeline/pricing.py)，2026-09-18 依 OpenAI 官方定價頁確認。
實際花費以 `data/<video_id>/manifest.json` 的帳本為準（`finvid costs` 可印）。

## 1. 單價表

| 項目 | 模型 | 單價 | 備註 |
|---|---|---|---|
| STT | `gpt-4o-mini-transcribe` | $0.003 / 分鐘 | 預設 |
| STT | `whisper-1` | $0.006 / 分鐘 | 有精確時間戳 |
| STT | faster-whisper（本機） | $0 | CPU 約 0.3–0.5× 實時 |
| LLM Pass A | `gpt-5-mini` | $0.25 / $2.00 per 1M tokens（輸入/輸出） | 預設 |
| LLM Pass B | `gpt-5` | $1.25 / $10.00 per 1M tokens | 預設，reasoning effort = low |
| LLM 替代 | `gpt-4.1-mini` | $0.40 / $1.60 | Pass B 省 5 倍 |
| TTS | edge-tts | $0 | 預設 |
| TTS | `gpt-4o-mini-tts` | $0.60 / 1M 字元 + 音訊 token ≈ $0.00005/字 | 可選 |
| AI 影片 API（對照用） | Runway / Kling / Veo 類 | ≈ $0.25 / 秒 | 流程不會呼叫，只算對照 |

## 2. 本片一次完整執行的估算與實際

素材 15 分 50 秒（950 秒）。

`finvid run --dry-run` 的輸出（不打 API）：

| 步驟 | 計費量 | 估算 |
|---|---|---|
| s1 下載+前處理 | 950 s → 948 s（去靜音只省 0.2%，見下方說明） | $0 |
| s2 STT `gpt-4o-mini-transcribe` | 15.8 分鐘 | $0.047 |
| s3 Pass A `gpt-5-mini` | ≈ 7k in / 1.5k out | $0.005 |
| s3 Pass B `gpt-5` × 3 | ≈ 1.2k in / 0.9k out 每段 | $0.032 |
| s4 TTS edge-tts | ≈ 600 字元 | $0 |
| **合計** | | **$0.083** |

實際花費寫在 `data/<video_id>/manifest.json`，`finvid costs` 可印；每一筆都有 usage 與單價。

### 對照：不做成本控制會花多少

| 情境 | 估算 |
|---|---|
| 整支送 `whisper-1` 而非 mini | 15.8 min × $0.006 = $0.095（貴 2 倍） |
| 不篩選，8 段全用 `gpt-5` 寫腳本 | ≈ $0.085（Pass B 貴 2.7 倍） |
| 3 支 × 35 秒用 AI 影片 API 生成 | 105 s × $0.25 ≈ **$26** |
| 8 段全生成 AI 影片 | ≈ **$70** |
| 同一支影片被重跑 5 次（無快取） | 5 × $0.08 = $0.40，有快取 = $0.08 |

結論：最大的一筆是影片生成，其次是強模型寫腳本。所以流程把「要不要生成」的判斷放在最前面（Pass A 打分 + 篩選閘），把生成本身換成零成本的程式化合成，並把 AI 影片 API 當可選項而非預設。

## 3. 每個決策的取捨

### 為什麼只拉音軌、去靜音，但不預設加速
- 只拉音軌：影片檔 50–100 MB vs 音軌 15 MB，下載時間與磁碟都省，STT 本來就不吃畫面。
- 去靜音：對純談話素材有效，對準確率幾乎無影響。**但本片實測只省 0.2%**：用 ffmpeg `volumedetect` 量到平均 −22.5 dB，全程有音樂床；要切到 14 秒以上得把門檻拉到 −22 dB，那會連人聲一起切。所以門檻留在安全的 −35 dB / 0.7 s，並在文件誠實記錄它對這支影片沒用。dry-run 估算裡的「省 8%」是談話節目的通用假設，對本片偏樂觀。
- 加速 1.2×：Whisper 系列在 1.2× 內中文準確率仍可接受，但會多錯字；demo 不值得為省 $0.008 冒風險。做成 `FINVID_SPEEDUP` 開關並在文件說明，比硬開更符合「合理判斷」。

### 為什麼 STT 預設走雲端而不是本機
- 本機 faster-whisper 是 $0，但第一次要下載模型、CPU 跑幾分鐘，評審機器不一定有 GPU。
- `gpt-4o-mini-transcribe` 整支 $0.04，用同一把 OpenAI key，安裝成本最低。
- 兩者都實作，`FINVID_STT_PROVIDER=local` 一行切換。

### 為什麼兩階段而不是一次叫強模型做完
- 逐字稿約 7k tokens，強模型讀一次 ≈ $0.01，便宜模型 ≈ $0.002。差 5 倍。
- 更重要的是「先知道哪幾段值得做」才能決定 Pass B 呼叫幾次。一次做完等於每段都付強模型的錢。
- 篩選閘是純程式，$0，而且規則可解釋（分數、重複、上限），評審看 `03_scripts.json` 就知道每段為什麼被跳過。

### 為什麼 Pass B 用 `gpt-5` 而不省
- 改寫品質決定兩件事：合不合法（反抄襲閘過不過）、值不值得看。
- 只對 3 段呼叫，總共 $0.03。省成 `gpt-4.1-mini` 只差 $0.025，但退回重寫的機率上升，重寫一次就把省的錢吃掉。
- 用 reasoning effort = low：改寫不需要長推理。

### 為什麼反抄襲閘用 n-gram 而不是再叫一次 LLM 判斷
- $0、確定性、可寫測試。
- 6 字元 n-gram：中文 6 字連續相同已經很難是巧合；數字與專有名詞（台北市、2.5%）短於 6 字不會誤判。
- 15% 重疊 + 最長 12 字：實測改寫良好的腳本落在 3–8%，照抄的超過 50%。
- 超標退回一次就停：第二次還抄代表模型對這段做不到，繼續重試是浪費。

### 為什麼影片生成不用 AI 影片 API
- 財經數據型內容的價值在數字與圖表，AI 生成畫面沒有附加價值，卻是全流程最貴的一步（一支 ≈ $9）。
- 程式化合成：TTS 免費、圖表 matplotlib、字幕 PIL、合成 ffmpeg，$0 且完全可控，也天然符合「圖表自製」要求。
- 帳本仍記一筆 reference 費用，讓對照可見。

### 冪等快取的粒度
- 以 stage 為單位，key = 影響輸出的設定 hash。改 Pass B 的模型只重做 s3、s4，不重做 STT。
- 輸出檔不在就視為未做，避免「manifest 說做過但檔案被刪」的假命中。
- 同一支影片在 web UI 同時只允許一個執行，避免並發重複計費。

## 4. 規模化時的成本

| 影片長度 | STT | LLM | 合成 | 每支 | 100 支/月 |
|---|---|---|---|---|---|
| 15 min | $0.04 | $0.04 | $0 | ≈ $0.08 | $8 |
| 60 min | $0.16 | $0.08（Pass A 較長，Pass B 不變） | $0 | ≈ $0.25 | $25 |
| 60 min，Pass B 改 `gpt-4.1-mini` | $0.16 | $0.03 | $0 | ≈ $0.19 | $19 |
| 60 min，STT 改本機 | $0 | $0.08 | $0 | ≈ $0.08 | $8 + 機器時間 |

規模化時先換的順序：STT 改本機（省最多、品質不掉）→ Pass B 降級（品質略降）→ 加速音檔（準確率略降）。
